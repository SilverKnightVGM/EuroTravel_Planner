<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prog2.eurotravelplanner" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FbEcryption"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".Context"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Destinos"
            android:label="Destinos" >
        </activity>
        <activity
            android:name=".Opciones"
            android:label="Opciones" >
        </activity>
        <activity
            android:name=".subdiviciones_opciones"
            android:label="@string/title_activity_subdiviciones_opciones" >
        </activity>
        <activity
            android:name=".DatosGenerales"
            android:label="Datos Generales" >
        </activity>
    </application>

</manifest>
